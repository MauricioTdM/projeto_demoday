server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # O Pulo do Gato: Se não achar o arquivo, devolve o index.html
        try_files $uri $uri/ /index.html;
    }
}